{
  "name": "monorepo",
  "devDependencies": {
    "turbo": "^2.3.4"
  },
  "private": true,
  "workspaces": [
    "backend-repo",
    "frontend-repo"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  }
}
